{
  "scripts": {
    "snapshot": "node scripts/backup-snapshot.js",
    "snapshot:local": "node -e \"require('dotenv').config({path:'server/.env'}) || true; process.env.BACKUP_SKIP_DRIVE='1'; require('./scripts/backup-snapshot.js')\"",
    "cleanup:orphan-members": "node scripts/cleanup-orphan-memberships.js",
    "backfill:summary": "node scripts/backfill-student-summary.js",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:run": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "axios": "^1.18.1",
    "bcryptjs": "^3.0.2",
    "exceljs": "^4.4.0",
    "firebase-admin": "^13.4.0"
  }
}
